# 库存 N/A 判定表

不进入总对照清单的库存项。每条必须有理由（spec §5.1："不适用"仅限 Electron/浏览器平台特有且 macOS 原生形态无对应用户价值）。

| 库存 ID | 理由 |
| --- | --- |
| web.page:pages/error/404.vue | 浏览器 hash 路由 catchAll 的兜底 404 页（pages/error/404.vue：显示"404 / 页面不存在 / 返回首页"按钮）。macOS 原生无地址栏，用户经按钮/标签导航无法抵达未知路由；程序性误导航由 GoRouter 默认错误页作为开发兜底，无对应原生用户价值。属浏览器 SPA 路由特有。 |
| web.route:/:catchAll(.*) | 与 404 页配对的浏览器 catchAll 路由入口（Toonflow-web/src/router/index.ts:5-12）。macOS 原生无用户可达的未知 URL 触发路径，同属浏览器 SPA 路由特有，无对应原生用户价值。 |
| web.page:pages/login/index.vue | ToonFlow 登录是浏览器 SPA 与本地 Electron HTTP 后端之间的 JWT 鉴权门：Toonflow-app/src/app.ts:152-170 对除 /api/login/login 外的全部 /api 路由校验 token，凭据存 o_user、签发 180 天 Bearer JWT。DramaFlow 为单用户原生应用、内嵌引擎无 HTTP 服务与会话，启动直接进入项目列表、无鉴权门（o_user 表在 app/lib/src/engine/db.dart:343 保留但 UI 不用于鉴权），登录鉴权在 macOS 原生形态下无用户价值。登录页仅有的非鉴权能力（语言切换、供应商请求地址配置）由设置页承接（app/lib/src/screens/settings_screen.dart:364-384 语言、:391+ 供应商配置）。 |
| web.page:views/test/index.vue | 开发者诊断页（Toonflow-web/src/views/test/index.vue：单个"测试按钮"调用 GET /test/test 并原样打印后端响应）。未被工作台任何菜单/导航引用，仅可手动改 hash 抵达，无面向最终用户的可观察能力；复刻它对用户零价值。**判定说明（2026-07-18 审查标注）**：本条仅满足 spec §5.1 两个必要条件中的"无对应用户价值"，不满足"平台特有"——它不是 Electron/浏览器专属机制（一个开发诊断页在任何平台都可能存在），只是恰好零用户价值。这是有意的"取精神舍字面"的例外（判绿/判缺失都不准确：DramaFlow 没有也不需要对应的开发诊断页），**需在 W0 范围确认时经用户明确追认**，不视为自动合规。 |
| web.page:views/production/components/workbench/generate copy.vue | 死代码：live 版 generate/ 目录的并行重复副本。经全仓 grep（src 内全部 .vue/.ts/.js）验证：generate copy.vue 及其 3 个嵌套文件在自身目录外零引用（无 import/from/动态 import/组件注册），从不被任何路由或组件挂载，故无任何用户可观察行为可复刻。与已排除的 scriptAgent/index copy.vue 同型（编辑遗留副本）。非 spec §5.1 的平台特有类，属"无用户价值的死代码"排除，逻辑对应的 live 能力由 generate/ 目录条目（W6B-GEN-*）承接。 |
| web.page:views/production/components/workbench/generate copy/index.vue | 死代码：generate copy.vue 死副本的嵌套 index，同上 grep 验证零外部引用、从不挂载、无用户可观察行为；对应 live 能力见 W6B-GEN-* 条目。 |
| web.page:views/production/components/workbench/generate copy/components/trackList.vue | 死代码：generate copy 死副本的嵌套组件，同上 grep 验证零外部引用、从不挂载、无用户可观察行为；对应 live 轨道能力见 W6B-GEN-TRACK-001。 |
| web.page:views/production/components/workbench/generate copy/components/videogenerate.vue | 死代码：generate copy 死副本的嵌套组件，同上 grep 验证零外部引用、从不挂载、无用户可观察行为；对应 live 生成能力见 W6B-GEN-VIDEO-001/W6B-GEN-CANDIDATE-001。 |
| web.component:setting/components/requestConfig.vue | 设置页「请求地址」面板配置 baseUrl（浏览器 SPA 访问其本地 Electron/Node HTTP 后端的地址，默认 http://localhost:10588；Toonflow-web/src/components/setting/components/requestConfig.vue:63-75 的「刷新」经 `toonflow://getAppUrl` 自定义协议向 Electron 取应用 URL）。**平台特有**：此「前端客户端→本地后端」寻址是浏览器 SPA + 本地 HTTP 后端（Electron）架构固有。**无原生用户价值**：DramaFlow 引擎在进程内内嵌、无 HTTP 服务/端口/客户端→服务寻址，用户无后端地址可配。两必要条件均满足。注意：此为应用自身后端地址，非 AI 供应商地址——供应商 baseUrl 由 DramaFlow 供应商配置承接（总清单 W6D-VENDOR-001），二者不同，不构成孤儿。 |
| web.component:setting/components/loginConfig.vue | 设置页「登录配置」修改浏览器 SPA↔Electron 后端 JWT 鉴权的账号凭据（用户名+密码，Toonflow-web/src/components/setting/components/loginConfig.vue:49-73 fetch getUser / updateUserPwd 走 o_user）。**平台特有**：该鉴权门（Toonflow-app 对除 /api/login/login 外全部 /api 校验 Bearer JWT，见既有 web.page:pages/login/index.vue N/A 条）本身即浏览器/Electron 客户端-服务端架构产物，修改其凭据仅在该门存在时有意义。**无原生用户价值**：DramaFlow 单用户原生应用、无 HTTP 服务/会话/鉴权门（o_user 表在 db.dart:343 保留但不用于鉴权，启动直入项目列表），无登录凭据可改。两条件均满足，与 /login N/A 同一理由。特别核实：这是本地后端自身鉴权凭据，非第三方/云同步账号登录（后者不受此理由覆盖），故不落「配置不同功能」的例外。 |
| web.component:setting/components/logoutConfig.vue | 设置页「退出登录」清除浏览器 localStorage 的 token/user（Toonflow-web/src/components/setting/components/logoutConfig.vue:40-51）并跳回 /login，是上述同一 JWT 鉴权门的浏览器 SPA 会话产物。**平台特有**：退出=清浏览器 localStorage 会话+回退登录门，属浏览器 SPA 会话机制。**无原生用户价值**：DramaFlow 单用户原生应用无 HTTP 会话可结束、无鉴权门可回退。两条件均满足，与 /login N/A 同一理由。 |
| web.component:setting/components/devConfig.vue | 设置页「开发者配置」三控件均为浏览器/Electron/Node 平台的开发者诊断，面板以「开发者设置」警告（Toonflow-web/src/components/setting/components/devConfig.vue:4）圈护：(1)「打开 DevTool」:134-144 经 `toonflow://openDevTool` 开 Chromium 开发者工具（Electron 特有）；(2)「AI DevTool 开关」:146-153 切换 Vercel AI SDK 的 devtools（ai-sdk.dev，Node 后端特有）；(3) localStorage 管理器 :155-300 对浏览器 `window.localStorage` 增删改查（浏览器 Web Storage 原语特有）。**平台特有**：三者所依赖底座（Chromium DevTools、Vercel AI SDK、浏览器 localStorage）在 Flutter/Dart 原生应用均不存在（DramaFlow 状态存 SQLite）。**无原生用户价值**：三者均为开发者调试用途、非终端用户功能，复刻任一对用户零价值。两必要条件对三控件分别成立、对整面板联合成立。（本条按批次指引就每一控件核验「确系平台底座绑定」而非仅「零价值」，避免误用 §5.1 例外。） |
| web.page:views/production/components/editImage/results.vue | 死代码：节点式图片编辑器的"生成结果"展示卡组件（results.vue：标题栏 + 全屏/下载图标[两个 icon 均无 @click，纯装饰] + 结果图或"等待生成"占位 + 标签）。经全仓 grep 验证：无任何 import（`editImage/results`、`./results` 零命中）、无全局组件注册，且 editImage 画布仅注册 node-upload/node-generated/edge-removeLine 三个模板（index.vue:23-32）而无 node-results/node-generateResults，故该组件从不被挂载、无用户可观察行为可复刻。与 6b 排除的 generate copy 死副本同型。非 spec §5.1 平台特有类，属"无用户价值的死代码"排除；其暗示的"生成结果展示"能力由 live 生成节点图片区（W6C-EDIT-GENERATED-001）承接。 |
| web.component:titleBar.vue | Electron 无边框窗口的自绘标题栏（Toonflow-web/src/components/titleBar.vue:2-17 交通灯式最小化/最大化/关闭按钮经 toonflow://windowMinimize·windowMaximize·windowClose IPC，:75 -webkit-app-region:drag 拖拽区），仅在 App.vue:6 `v-if="isElectron"` 时渲染。**平台特有**：无边框窗口 + 自绘窗口控件 + toonflow:// IPC 是 Electron chrome-less 窗口机制。**无原生用户价值**：macOS 原生窗口自带标题栏与红黄绿交通灯窗口控件，DramaFlow 用系统原生窗口装饰、无须自绘。两必要条件均满足。 |
| web.store:user.ts | 浏览器 SPA 会话 token 持有（Toonflow-web/src/stores/user.ts:4 从 localStorage 读 JWT token 并持久化），用于对本地 Electron/Node 后端的 Bearer 鉴权。**平台特有**：该 token 即浏览器 SPA↔Electron 后端 JWT 鉴权门（见既有 web.page:pages/login/index.vue N/A 条）的会话凭据持有者，属浏览器/客户端-服务端架构产物。**无原生用户价值**：DramaFlow 单用户原生应用、内嵌引擎无 HTTP 服务/会话/鉴权门（o_user 表在 db.dart:343 保留但不用于鉴权、启动直入项目列表），无 token 可存。两必要条件均满足，与 /login、loginConfig 等 N/A 同一理由。 |
| web.featureLib:utils/axios.ts | 浏览器 SPA 访问本地 Electron/Node 后端的 HTTP 客户端（Toonflow-web/src/utils/axios.ts:7-19 baseURL=settingStore.baseUrl[http://localhost:10588/api]、注入 Authorization token、:26-30 401→跳登录、:31-68 到本地后端的 Network Error 提示含 Windows「以管理员运行/装 VC++ 运行库」引导）。**平台特有**：这是浏览器前端→独立本地 HTTP 后端（localhost:10588）的传输层，属浏览器 SPA + 本地后端架构固有。**无原生用户价值**：DramaFlow 引擎进程内内嵌、无 HTTP 服务/端口/会话 token/到 localhost 的网络失败，所有「请求」为进程内 Dart 调用。两必要条件均满足。（AI 供应商 baseUrl 是另一概念、由供应商配置 W6D-VENDOR-001 承接，不构成孤儿。） |
| web.featureLib:utils/useSocket.ts | 浏览器 SPA 到本地后端的 socket.io WebSocket 封装（Toonflow-web/src/utils/useSocket.ts:20-57 io(url) 连本地后端、token 鉴权、connect/disconnect/send/on）。**平台特有**：浏览器 socket.io-client→独立本地 WS 服务，属浏览器 SPA + 本地后端架构固有。**无原生用户价值**：DramaFlow 内嵌引擎进程内流式（assistant_chat.dart），无 WS 服务/客户端；且经全仓 grep 该封装当前零引用（已被 useChat 内联 socket 取代、非 live）。两必要条件均满足。 |
| web.featureLib:utils/useChat copy.ts | 死代码：useChat.ts 的编辑遗留并行副本。经会话内 grep 复核（grep -rln "useChat copy" src --include=*.vue --include=*.ts 仅命中自身、零外部 import/from/动态导入/注册）验证从不被挂载、无用户可观察行为可复刻。与已排除的 scriptAgent/index copy.vue、generate copy 死副本同型。非 spec §5.1 平台特有类，属"无用户价值的死代码"排除；对应 live 能力见 W6E-STORE-AGENT-001（useChat.ts）。 |
| app.route:setting/getTextModel.ts | 挂载但零调用的死桩路由（Toonflow-app/src/routes/setting/getTextModel.ts:5-9：POST 恒返回硬编码 success("123")，无任何 DB/逻辑）。经 grep 核实 Toonflow-web/src 无任何调用（前端零引用），Toonflow-app 内除 src/router.ts 自动注册（:137/:308）外无引用（data/web/index.html 的 getTextModel 命中为无关的 Monaco 编辑器 API）。无任何用户可观察行为可复刻。**判定说明**：本条仅满足 spec §5.1 两必要条件中的「无对应用户价值」，不满足「平台特有」——一个返回常量的调试桩非 Electron/浏览器专属机制。属与 6d/6b「无用户价值的死代码」（views/test/index.vue、generate copy.vue 等）同型的有意「取精神舍字面」例外，需在 W0 范围确认时经用户明确追认，不视为自动合规。 |
| app.route:login/login.ts | ToonFlow 登录后端鉴权端点：Toonflow-app/src/routes/login/login.ts:17-46 校验 o_user 用户名/密码、经 o_setting.tokenKey 签发 180 天 Bearer JWT，是浏览器 SPA↔本地 Electron/Node HTTP 后端鉴权门的签发端（app.ts:152-170 对除 /api/login/login 外全部 /api 校验 Bearer JWT）。**平台特有**：该 JWT 鉴权门本身即浏览器 SPA+本地 HTTP 后端（Electron）架构产物。**无原生用户价值**：DramaFlow 单用户原生应用、内嵌引擎无 HTTP 服务/会话/鉴权门（o_user 表在 db.dart 保留但不用于鉴权、启动直入项目列表），无登录可鉴权。两必要条件均满足，与既有 web.page:pages/login/index.vue、loginConfig/logoutConfig/user.ts 等 N/A 同一理由（本条为其后端签发端）。 |
| app.route:test/test.ts | 开发者诊断端点：Toonflow-app/src/routes/test/test.ts:6-12 GET 恒 return "ok"（其后读 o_vendorConfig 写 test.json 为 return 之后的死代码、永不执行），供开发者诊断页 Toonflow-web/src/views/test/index.vue 的"测试按钮"打印后端响应。未被工作台任何菜单/导航引用，无面向最终用户可观察能力。**判定说明**：本条仅满足 spec §5.1 两必要条件中的"无对应用户价值"、不满足"平台特有"——一个返回常量的开发诊断端点非 Electron/浏览器专属机制。属与既有 web.page:views/test/index.vue（其配对前端页）、app.route:setting/getTextModel.ts 同型的"取精神舍字面"例外，需在 W0 范围确认时经用户明确追认，不视为自动合规。 |
