# v0.4 收敛期执行台账（plan: docs/superpowers/plans/2026-07-08-v0.4-convergence.md）
起点 BASE: e5169fe
排期修正: T5 严格在 T3 合入后（同文件 workbench_screen.dart）
Task 1: complete (commit 03823a4, review clean; Minor×3: 测试setUp重复/未close db/未知taskClass+destructiveKey组合分支未测)
Task 4: complete (commit HEAD, 21 基线+全量904绿; 待 T6 等价性矩阵二次验证)
T9先于T8执行（T8分发引用笔记API），接口不变
Task 9: complete (T8 前置，6 测绿)
Task 2: complete (commit f1304f3, review clean; Minor×3: autoMode路径无helper级测试/遮罩关闭路径未测/开关即时生效与同区保存钮不一致)
Task 3: complete (8个花钱UI入口接闸; analyze绿; 全量938测绿; 从Claude失败worktree恢复并合入)
Task 8: complete (12测绿; AssistantAction 增 taskClass 字段供 T10 查闸; T11 先于 T10 执行——T10 系统提示词依赖 assistantSkillContexts)
Task 11: complete (先于T10, 8测新增938全绿; 旧文件未动)
Task 10: complete (assistant_chat 对话循环+确认挂起; 7测新增945全绿; 旧文件未动)
Task 12: complete (UI切到 assistant_*; 去除 custom-JS/监督/RAG 入口; 项目笔记走 project_notes; analyze绿; 全量931测绿; 旧 agent.dart 未动)
Task 13: complete (删除旧 agent/agent_memory/agent_orchestrator/agent_skills/agent_stage_registry + 旧 agent_test; 旧符号扫描仅剩 db.dart schema 定义 o_memoryVector; assistant_* + project_notes 共 1395 行; analyze绿; 全量479测绿)
Task 5: complete (workbench 切分新尾段保留选中; 批量裁剪忽略选中自撞; 拖拽 helper 单手势复用时间线快照; workbench 80测绿; analyze绿; 全量480测绿)
Task 6: complete (新增时间线单/批量等价性矩阵; 发现并修复 resizeTimelineClipsEndRipple 相邻选中裁尾时下游按单片段缩短量重复叠加的净位移 bug; timeline定向31测绿; analyze绿; 全量490测绿)
Task 7: complete (补 policy_confirm UI 层 autoMode 矩阵: 花钱自动放行、破坏自动仍确认; project_notes 中文检索用例已覆盖计划项并随本卡验收; 定向15测绿; analyze绿; 全量492测绿)
Task 14: complete (workbench 两处 _toast 收敛为共享 SnackBar helper; 候选生成中/失败状态改用 StatusChip; grep _toast=0; workbench 80测绿; analyze绿; 全量492测绿)
Task 15: complete (进度文档改为 v0.4 assistant 降级版现状; ES-DSL/JS执行/多层监督/向量RAG 标注已移除; grep 命中9处; docs测试3测绿)

# ToonFlow Core Parity C2（plan: docs/superpowers/plans/2026-07-10-c2-prompt-packs-and-provenance.md）
C2 Task 1: complete (commits 28bb476..7e05a6c; 23 focused tests green; analyze clean; independent re-review approved)
C2 Task 2: complete (commits febd8e8..9bd1322; deterministic source resolution; focused tests green; analyze clean; independent re-review approved)
C2 Task 3: complete (commits 64d7dc6..4ec713d; queue task carries deterministic source unions plus per-target request traces without raw prompt text; private instruction payload is local-only and fails closed when missing or hash-mismatched; independent final re-review approved)
C2 completion gate: complete (final code 4ec713d plus tamper regression follow-up; focused prompt tests green; full suite, analyze, and macOS debug build green before the review follow-up; C3 boundary scan clean)

# ToonFlow Core Parity C3（plan: docs/superpowers/plans/2026-07-10-c3-seedance-capability-video.md）
C3 Task 1: complete (commits c97bc14..fc30a4c; explicit capability parser/request fingerprint and conservative Seedance profiles; 28 focused tests green; analyze clean; task re-review accepted. Cross-task gate: Task 4 must route runtime video submission through this validator.)
C3 Task 2: complete (074b466; v10 additive request/upstream storage, opt-in cold-start recovery, versioned per-shot controls, and effective prompt provenance; combined review's runtime integration finding resolved by Task 4.)
C3 Task 3: complete (5218473; typed Seedance submit/poll/cancel, kind-safe binding resolution, protocol-correct image/video/audio references, and Bearer-prefix regression coverage; combined review's runtime integration finding resolved by Task 4.)
C3 Task 4: complete (853e54d; project-model request build/preflight, persisted accepted IDs, no-resubmit cold recovery, upstream cancellation plus race protection, assistant/workbench same path; full 536 tests, analyze, macOS debug build green; independent review found no Critical/Important issues.)

# ToonFlow Core Parity C4（plan: docs/superpowers/plans/2026-07-11-c4-production-dependencies.md）
C4 Task 1: complete (62e2f16; additive v11 dependency-state table, source hashes, stale propagation from plan/table/script and asset-link updates; focused tests, analyze, full Flutter suite green; independent task reviewer unavailable because the local Codex agent quota was exhausted, controller performed diff review.)
C4 Task 2: complete (736db34; optional text model templates preserve strict C3 video behavior, C4 stages have mobile/desktop defaults, queue/policy/resolver/settings registration; focused tests, analyze, full Flutter suite green; independent task reviewer unavailable because the local Codex agent quota was exhausted, controller performed diff review.)
C4 Task 3: complete (director-plan task snapshots project script hash, resolves the documented prompt stack, records provenance without raw script text in task JSON, strips think blocks, and refuses missing/stale input before gateway submission; focused tests, analyze, full Flutter suite green; independent task reviewer unavailable because the local Codex agent quota was exhausted, controller performed diff review.)
C4 Task 4: complete (76f1167; storyboard-table generation snapshots plan/script/assets, records ordered provenance without raw source text, stores editable Markdown, and marks structured shots stale; focused tests, analyze, and full Flutter suite green.)
C4 Task 5: complete (strict Markdown table parser; table-authoritative shot metadata; pre/post-provider source and existing-output race guards; validated savepoint replacement with rollback; reusable media protection; 574 full tests and analyze green; independent re-review Ready.)
C4 Task 6: complete (dependency-aware plan/table/storyboard controls; stale badges; replacement then money confirmations; task labels and zh/en/ja copy; script changes invalidate the director plan; 580 full tests, analyze, and macOS debug build green; independent re-review Ready.)
C4 Task 7: complete (assistant generate_scripts delegates to the existing event-to-script queue; structured-shot assistant path is table-driven and refuses replacement; fake-provider C4 chain verifies source hashes and excludes credentials/raw prompt bodies from task JSON; 583 full tests, analyze, and macOS debug build green; independent re-review Ready.)

# C5 Episode Close And Real Acceptance（plan: docs/superpowers/plans/2026-07-11-c5-episode-close-real-acceptance.md）
C5 Task 1: complete (failed tasks retain their reason and become immutable history when retried; each retry is a new visible attempt with lineage metadata; private prompt payload moves atomically to the latest attempt; focused 46, full 583, and analyze green; independent reviewer could not complete because Codex quota was exhausted, controller completed transaction-boundary review.)
C5 Task 2: complete (accepted running Seedance candidates resume polling without submit; terminal failures and corrected prepared requests create one new candidate; uncertain submissions refuse retry; legacy tasks without videoIds retry only each track's latest failed candidate; successful retry selects a usable candidate; focused video tests, full 588, and analyze green; controller diff review.)
C5 Task 3: complete (shared transition/filter allowlists validate engine export, default composer, and AVFoundation channel wrapper; unknown effects fail before native export rather than silently dropping; focused composition/platform tests, full 592, and analyze green; controller diff review.)
C5 Task 4: complete (macOS AVFoundation Engine-to-MP4 integration test covers reverse-created then reordered selected candidates, bound narration, fade, inspectable MP4 output, and registered clip asset; discovered and corrected the fade route from the hanging Core Image callback path to existing AVFoundation layer instructions on both macOS and iOS; native integration, full Flutter suite, analyze, and macOS debug build green.)

# W0 基线审计（plan: docs/superpowers/plans/2026-07-18-w0-parity-baseline-audit.md）
起点 BASE: dca83dd（develop）；worktree ../dramaflow-w0 分支 w0-audit；Task 6-11 串行（共写 master-checklist.md）

# P0 供应商预检（plan: docs/superpowers/plans/2026-07-18-p0-provider-preflight.md）
起点 BASE: dca83dd（develop）；worktree ../dramaflow-p0 分支 p0-preflight；Task 4/5（azt 冒烟+真实调用）主会话执行，不派子代理
W0 Task 1: complete (5941dea; 脚本与简报逐字一致经程序化 diff 验证; manifest 473+249 文件哈希结构自洽; 验证 1.1.8/9c4cb0e/true/true dirty=false; review clean. Minor×2: 实现报告行数记账有误/walk 对 symlink 与不可读文件静默跳过——基线若引入 symlink 会少计,留意后续)
P0 Task 1: complete (e2b2272; 两脚本与简报字节级一致(独立diff验证); 日志路径穷举证明无 headers/body/query 泄漏通道; node --check 绿; review clean. Minor×4(均简报代码固有,非实现者裁量): hop-by-hop 头透传/Buffer 分块 UTF-8 拼接隐患/入站 req 无 error handler/实现报告漏述 mark 日志形态)
P0 Task 2 中途发现: 自测门抓到计划参考代码 bug——fake_upstream 用原始 req.url 路由,带 query 的提交 POST 404(自测故意带 ?debug=1 验证代理去 query)。属计划自身缺陷,非实现者错误;修复卡已派(pathOnly 路由+计划文档同步修正)
W0 Task 2: complete (797e56d; 脚本与简报逐字一致(diff -u 0差异); 提交产物经叶级遍历+正则扫描零秘密值; 13 vendors 全部通过白名单结构断言; selftest 独立复跑 PASS; review clean. Minor×3: volcengineSd2 是当前唯一实测秘密键排除路径的 vendor/源数据有 id="null" 字符串 vendor(下游黑盒会话留意)/modelPrompts 投影因表空未实测)
P0 Task 2: complete (d3a9214+9a35bff; 自测抓到并修复假上游 query 路由 bug(计划文档同步修正,两处 hunk 字节一致); selftest 13 断言全 PASS; sentinel=代理文件 SHA-256 落盘; 审查逐字节核验 approved. Minor×2: 报告文件缺原 BLOCKED 记录文本/phase-2 未复查假上游 pid(简报代码固有))
W0 Task 3: complete (cc0f20f; 538 项库存,15 类计数全部被审查者独立对源复算一致,零漏生成; 脚本与简报逐字一致; review approved. 简报级发现×2(控制器裁定,不改产物): ①items 无 kind 字段(简报散文与代码自矛盾)→下游一律按 id.split(':')[0] 推导,Task4 校验器本就如此; ②web.route:/detail 是注释掉的死路由(正则不剥注释)→Task 6 审计时按"不适用+死路由理由"进 inventory-na.md,不得费力审计. Minor: ToonFlow 源里多个" copy"文件属上游 dev 残留,如实收录)
W0 Task 4: complete (d6914c8; 三文件与简报字节级一致(diff 验证); TDD序列538→537→538在独立scratch副本复现; 五条校验器语义逐一执行验证(覆盖列索引/NA缺理由error+exitCode穿透/未知id仅WARN/孤儿列表+exit1/全覆盖OK+exit0); review approved. Minor: 简报参考实现对"行首缺|"边界未防御,当前0数据行未触发,Task6+写真实行后留意)
P0 Task 3: complete (151bd91; 两用例语义经引擎源码交叉验证为真(video_track.dart:936-963 switch路由 uncertain→_throwUncertainVideoRetry / accepted+终态失败→新候选); 授权偏差仅4个extension import+dart format换行,逐行核验; 默认套件零副作用经目录mtime前后比对独立复现; analyze 0 issues; review approved. Minor: 报告措辞不够精确(非代码问题)
P0 Task 4: complete (无提交,纯服务冒烟); azt 文本 2.2s 返回OK; 图片 b64_json 长度1171768(>10000门槛) 耗时26.7s,远快于AGENTS.md历史195-342s记录(模型/负载差异,非异常); /tmp/p0-azt-smoke.txt 留证
W0 Task 5: complete (f078961; 脚本与简报 SHA-256 逐字一致; 夹具自测(mktemp隔离,含二次隔离阻断/restore丢弃隔离期污染数据验证)全过; 真实目录 backup+verify(1144文件,1项目)只读证据链完整,LIVE 未被触碰; review approved. Important×2(简报锁定设计固有,休眠中): ①restore 靠 $ASIDE 是否存在判隔离态,无会话/时间戳校验——若某次 isolate 后会话被遗弃、真实新数据又在 $LIVE 长出来,后续 restore 会先 rm -rf 掉新数据再覆盖旧快照;②assert_quit 只精确匹配进程名 ToonFlow,抓不到孤儿 Helper/Renderer 子进程. **硬性前置纪律**:今后任何审计单元(Task 6-10)如果真的需要调用 isolate(而不只是 backup/verify),必须先手动确认 `$HOME/Documents/dramaflow-w0-blackbox/live-aside` 不存在(证明没有遗弃的隔离态)且用 `ps aux | grep -i toonflow` 确认无残留 Helper 进程,再调用,不得默认脚本自身的检查已经足够
P0 Task 5 尝试1: 真实调用已发生(exactly-once纪律成立,proxy日志仅1次POST)。供应商 HTTP 400 拒绝(10.99s),o_video落 uncertain+errNetwork,o_tasks落 failed。根因诊断:测试参考图 864x1821(比例0.475)与请求 videoRatio=9:16(比例0.5625)不匹配——测试夹具问题非引擎/协议缺陷。GATE1未过,脚本按设计正确停手未进入RESTART/resume,未重试。已征得用户授权对同一预检做第二次真实尝试(裁剪出精确9:16参考图,原图未动,harness路径更新)。尝试2 进行中。
P0 Task 5: 真实调用已完成两次(均用户授权),均未达成生成成功。尝试1(原图,比例0.475 vs 请求9:16=0.5625)→400。假设比例不符,征得用户授权重试。尝试2(裁剪至精确9:16)→仍400,假设证伪。两次均恰好1次POST(代理日志核验),GATE1(硬杀点到达)在两次都正确拒绝进入RESTART/resume,未发生任何重复提交。根因未知(代理设计不记录响应体+引擎错误处理只存dio通用消息,真实拒绝原因永久丢失)。经用户明确决策(本轮对话):视频供应商联调问题暂搁置不再排查。断言 2/6 PASS(提交次数=1/候选行数=1 两项安全性断言成立), 4/6 FAIL(无成功生成)。P0 期间零产品代码改动。已撤销脚手架路径的图片试验性改动,harness 回到已提交状态。证据文档 f3817eb 如实记录全过程,防泄漏自检两次假阳性(task-/acceptThenFail 均非密钥)人工核实排除。
W0 Task 6a: complete (4c56571+f4d1fba; 13顶层页面/路由,7绿+3部分实现+4不适用; 独立审查7/13逐项开源码文件核实claim真实性+N/A双条件逐一核验; approved但2条Important已修正: /test N/A补充"仅满足无价值prong,不满足平台特有prong,需W0范围确认时用户追认"说明; W6-WORKBENCH-001(script项目隐藏novelOnly项分支)补审查标注挂已知测试缺口而非静默维持无保留绿). production 页正确封顶部分实现(有widget测试但画布手感是W1开放里程碑,判绿会与W1自相矛盾)——评审确认这是正确判断不是过度保守。
W0 Task 6b: complete (6079e3a; 20行,11绿+8部分实现+1缺失(poster节点真缺失,旧清单未发现的新发现); 独立审查9/20逐项开双侧源码+测试体核实,poster用grep独立确认零实现,NLE特性数量(6vs3转场/10vs4滤镜/8vs0特效)逐一核对源码确认; approved,3条Minor(数字标注误差)已修正commit 4843592). 
W0 Task 6c: complete(内容层面; 6项,1绿(removeLine)+4部分实现+1不适用(results.vue死代码)). **流程事故**:我在6c子代理仍在编辑同一 master-checklist.md 时对该文件做了自己的修正提交(4843592),导致6c的5行W6C-*内容被并发扫入4843592而非6c自己的提交a1b7420(git -S pickaxe确认)。内容验证无损失(5行齐全/orphan gate对6c范围EMPTY/6c自己commit仅剩inventory-na.md 1行)——纯提交归属记录不准,不重写历史,如实记录。**教训**:今后在 dramaflow-w0 worktree 对 master-checklist.md/inventory-na.md 的任何自行编辑,必须等所有并行审计子代理(当前6d/6e在飞)确认落盘提交后再动手,不得假设"小修正"可以插队并发。
QA真实全链路修复(storyboard boolean parser): complete(82a7f07 fix + f462e92 test harness; review approved 19/19测试独立复核). 真实bug: base prompt(engine.dart)只命名"生成首帧"列从不规定取值格式,解析器又只认硬编码枚举,真实调用gpt-5.6-luna时输出了解析器不认的token导致整张分镜表报废。双重修复:解析器宽松匹配+无法识别时降级默认true(不再炸整表)+提示词加约束。已用真实全链路重跑验证修复生效(结构化分镜阶段从失败→23个镜头成功)。**跟进项(评审标注非阻塞)**:兜底逻辑是精确token匹配非子串,LLM若写含"不需要"的完整长句(非精确token)仍会掉入默认true分支触发非预期付费生图;建议后续给false分支加子串包含判断或对fallback分支加日志观察真实分布。
QA全链路最终结果: 修复生效后完整验证通过 项目→小说导入→事件生成→事件转剧本→资产提取(7个真实资产)→真实润色+真实生图(1.96MB)→真实导演规划(4468字符)→真实分镜表(4513字符)→真实结构化分镜(23个镜头,证明boolean parser修复生效)。首帧图批量生成(23张)在我自己测试脚本的_waitTask硬编码10分钟等待上限处超时——这是测试脚本自身超时设置对23张真实图片批量生成偏保守(单图历史记录最长5分钟+,23张全部完成可能需要更长),不是产品bug;且发现_waitTask的allowFail逻辑未覆盖"超时"分支只覆盖"任务显式failed"分支,是测试脚本自身设计缺口(非产品代码,不影响提交)。核心结论:全链路(小说→事件→剧本→资产→生图→导演规划→分镜表→结构化分镜)用真实 azt(gpt-5.6-luna文字+gpt-image-2图片)验证通过,证明该链路真实可用。批量生图/配音绑定/视频生成 未在本轮验证到 100% 完成(视频按指示搁置;批量生图受测试脚本超时限制未追完;配音绑定在链路里但因批量生图先超时未执行到)。
注意: 约04:xx后 Mac 屏幕自动锁屏(密码输入界面)。不会尝试解锁。已获取的证据不受影响: mac-01-launch.png(macOS真实渲染,锁屏前)、ios-01-launch.png(iOS模拟器真实渲染)均已留存;后台 flutter test 真实全链路是无头进程,锁屏期间持续正常跑网络请求并完成(结构化分镜23个镜头成功证实)。锁屏后不再进行任何需要屏幕交互的验证。
W0 Task 6 全部完成(6a-6e,5批次全部独立审查approved): 6a(f4d1fba,13顶层页面,7绿/3部分/4NA)、6b(6079e3a,20行workbench簇,11绿/8部分/1缺失,poster节点新发现)、6c(a1b7420,6项editImage/chat簇,1绿/4部分/1NA-死代码)、6d(e3b6498+7814811修正,23项设置簇,2绿/13部分/4NA,发现RAG调优面板完全未接线)、6e(e8a774a,30项共享组件/store/featureLib,1绿/2缺失/17部分/5NA-死代码,hello.vue首次运行向导确认真实缺失非误判). Task 6 覆盖 web.* 全部538项库存中的~180项。下一步 Task 7(app.route+app.socket,171项,拆约6个子批次7a-7f),7a(设置后端路由47项)已派发(首次尝试子代理故障0工作量,已重新派发中).
W0 Task 7a: complete(1201541; 47项设置后端路由,3绿/29部分/10缺失/5NA; 独立审查10项深查零引用错配; approved). 关键真实发现: ①dbConfig 后端级 clearData 是真正的全库出厂重置(drop全表+initDB重建,无排除清单),而DramaFlow是业务数据清空+保留配置(相反设计),export/import同理ToonFlow全库vs DramaFlow仅配置——比6d前端级发现的差异更精确(字节级验证); ②getTextModel.ts 是ToonFlow自己代码里挂载了但零调用方的死桩(返回字面量"123"),已按死代码同例外处理进inventory-na.md并标注需用户追认; ③agentSetKey.ts(ToonFlow自家商业Claude代理服务一键接入)评审确认"缺失"判定正确、不得自行判N/A(spec §5.1 N/A仅限平台特有,没有"第三方商业服务"这个例外类目),已挂备注等W0范围确认时用户裁决是否要复刻这类商业服务对接。
W0 Task 7b: complete(9ce0a5e; 40项生产后端路由,9绿/11部分(按行),22绿/18部分(按id); 独立审查11项深查零引用错配; approved). 关键发现: ①分镜预览导出真实gap——ToonFlow downPreviewImage 用sharp把多个镜头合成单张带标签网格图,DramaFlow只是逐个导出单张S0X文件,代码注释甚至写着"对齐downPreviewImage"但实际不对齐; ②级联删除数据完整性问题(真实但低紧急度,评审明确"不需要今晚修,是数据卫生非崩溃/丢数据路径")——DramaFlow的deleteAssets/deleteStoryboards从未删除o_imageFlow(全仓grep确认zero DELETE),deleteAssets也不清理assetId维度的o_assets2Storyboard(storyboardId维度有清理);实际影响低(下游走INNER JOIN会自动过滤悬空引用,orphan行只是静默堆积不会被误读);assets_test.dart的级联测试从未覆盖flowId/关联场景,是真实的测试覆盖缺口非"测试该抓到但没抓到". ③**待处理Important**: addTrack 在6b(判已验证等价)与7b(判部分实现)存在真实矛盾——ToonFlow track.vue:196-209 的addTrack是真实的"新建无分镜关联的独立轨道"能力(带duration),DramaFlow ensureTrackForStoryboard严格1:1lazy per-shot,无此路径;评审给出明确证据链(非需黑盒),结论7b对6b过宽,需收窄6b的W6B-GEN-TRACK-001备注或降级,并交叉引用W7B-WB-ADDTRACK-001。等7c落地后处理(避免并发覆盖)。
W0 Task 7c: complete(2ba3dda; 20项素材中心后端路由,8绿/2部分,0缺失/0NA; 独立审查approved零Critical/Important). 关键结论: 级联删除在这批"干净"——ToonFlow自己的delAssets/batchDelete本就不触o_assets2Storyboard/o_imageFlow(级联只在7b的deleteAssetsDireve),两边此处同样不做级联,非新gap;DramaFlow deleteAssets在这批实为超集(多删子资产图片+文件+o_scriptAssets);发现并确认ToonFlow自身delImage.ts有真实潜伏bug(先删行后查filePath,文件永不落删),DramaFlow未复制此bug(先查path再删)且不判更优(spec不要求复制历史bug)。队列4条纯cosmetic Minor待批量处理(不影响任何判定): DELETE行"无孤儿"措辞过强(有一处imageId空值清理未复制,但不可达/不影响判定)/报告用例数16实际14(行号引用本身准确)/CANCEL行引用的是polish取消测试非image取消测试(已在行内诚实注明)/check_no_orphans.js对转义竖线`\|`处理有原生缺陷(已用全角｜绕过且与既有约定一致,非本批引入新问题).
W0 Task 7d: complete(9283f9d; 26项项目+小说后端路由,24绿/2部分,0缺失/0NA; 独立审查approved,判定"边缘但站得住脚"). **第三个真实数据完整性发现**(DramaFlow自身bug,非对标ToonFlow差距): deleteProject(engine.dart:893-945)绕过deleteNovels直接删o_novel,从不清理o_event/o_eventChapter;o_eventChapter无FK cascade(db.dart:181-185,对比o_memoryVector确实有cascade,证明并非全局无FK设计);projects_test.dart:138的级联测试从未插入这两张表,是真实覆盖缺口。评审确认bug真实且当前对用户不可见(两处读取者events()和scripts.dart均走INNER JOIN过滤孤儿,评审补充发现报告漏引了scripts.dart这第二个读取者)——按spec §2"仅论用户可观察行为"的等价定义,不可见的内部完整性bug不强制拉低parity判定,已验证等价维持,备注留痕,归入W4长尾清零待修(连同7b的两处级联缺口,共3处真实数据卫生问题排队)。另外确认DramaFlow修了两处ToonFlow自身bug且未判更优(delNovel错误按junction行id而非eventId删事件;event列表页ToonFlow从未写入o_event/o_eventChapter故永远空,DramaFlow真正做出功能)。待处理: 备注补引scripts.dart第二读取者(等7e落地后处理,避免并发).
W0 Task 7e: complete(a20258d+2b46c12修正; 20项剧本/剧本Agent/塑角造景/画风后端路由,8绿/1部分/1缺失,0NA; 独立审查approved). 结算6a遗留问题:塑角造景批量生图无独立后端路由,页面按钮直调共享/assetsGenerate/batchGenerateImageAssets(与7c W7C-GEN-IMAGE-001同一路由),故迁移至素材中心是纯前端重组、后端引擎路径完全一致→6a的W6-CORNERSCAPE-002迁移等价性问题正式关闭。剧本删除级联本批"干净"(与7b/7d的3处真实缺口形成对比,DramaFlow反而是超集且修了ToonFlow自己的.mp4文件未删的documented leak)。唯一修正:结算行引用的前端行号有误(:477/:297实际是:723/:543,结论本身审查独立验证正确)已修正。
W0 Task 7f: complete(9cb4ac9; 18项(task/general/other/modelSelect/agents/test/login/common路由簇+2个app.socket),12绿/4部分/2NA; 独立审查approved,零Critical/Important). **Task 7 全部完成**(7a-7f串行6批次,171项app.route+app.socket,独立审查各自approved;本批独立复跑check_no_orphans.js确认app.route/socket孤儿=0,与171项总数吻合). 关键发现: ①general/getSingleProject+general/updateProject+general/generalStatistics+other/deleteAllData 均为前端零调用死路由——已用ToonFlow-web源码grep+打包版arbiter bundle(index.html)双重交叉核验,其中deleteAllData有前端同名函数dbConfig.vue:286但只开确认弹窗、真正调用的是setting/dbConfig/clearData(7a已收录),属函数名撞车陷阱,审查确认未被误判;②两个app.socket文件(scriptAgent/productionAgent)实为JWT鉴权的agent聊天流(ResTool增量流式content:add/thinking/toolcall),非dispatch brief猜测的任务进度广播,DramaFlow对应assistant_chat.dart(聊天)+queue.dart(独立的真实进度总线)双锚点经审查确认均成立且不混淆;③本批deleteAllData与7a的dbConfig/clearData是不同文件不同id(非重复审计),两行判定一致不矛盾,共享同一隐私分歧结论(DramaFlow保留配置vs ToonFlow工厂重置);④login.ts/test.ts两条N/A逐文件双prong核验(test.ts仅满足1/2,如实标注需用户追认,与getTextModel.ts/views/test/index.vue同例外处理)。4条Minor均为报告措辞层面(非master-checklist.md本身),不需修正提交。
W0 Task 8: complete(4b9a676; 7项app.agentTool+app.agentUtil,0绿/3部分/1缺失,0NA,另加1条W2-EVIDENCE-非评分留证行; 独立审查approved,零Critical/Important). 本单元三个Agent决策层子代理(productionAgent 7个子Agent工具/scriptAgent 4个子Agent工具)与三层记忆系统(自动embedding+摘要+向量RAG+deepRetrieve)在DramaFlow均只有部分/零对应(DramaFlow v0.4助手是共享13动作扁平注册表,无按family区分工具集、无实时画布读写、无子Agent委派架构),全批零已验证等价,经审查确认"评分方向没有错、评级本身由证据驱动"(DramaFlow自身代码注释多处明载v0.4刻意降级,非审计过严)。**关键判断**: W8-AGENTUTIL-MEMORY-001(向量记忆子系统)判"缺失"而非"部分实现"——审查独立复核(未采信实现者原推理、自行重新论证)给出更强理由:此行需与已判绿的W7F-AGENT-MEM-READ-001(读取历史消息)做互斥切分,后者已吸收"回放历史"这块价值且明确注记"摘要/RAG属W2另计",若本行再对project_notes.dart的词法笔记搜索判部分实现即构成对同一价值的重复计分(double-counting)——判定"缺失"实为避免此重复、而非过度保守。**W2-EVIDENCE-AGENT-JSDSL-001留证行重要发现**: brief指定的字面grep(custom-js/new Function/queryPlan)在全仓零命中,但补充排查发现ToonFlow供应商适配器子系统(src/utils/vm.ts)确有基于vm2的真实JS解释器,6处调用点含模型分发热路径(ai.ts:127)与两条用户可提交TS代码的HTTP路由(updateCode.ts/addVendor.ts)——与已合入的W7A-VENDOR-CODE-001/W6E-LIB-VENDORTPL-001两行(均判缺失)属同一事实的独立二次确认非新发现非矛盾,行内如实建议(非行动)未来W2 spec正式文字应把"禁止JS解释器"范围限定为"Agent工具调用面"以避免与供应商适配器子系统表述冲突。经审查逐一开双侧源码复核vm.ts全部6调用点+两行前情行均属实,行的措辞"honest, hedged, correctly cross-referenced, no overreach"。2条Minor均为DramaFlow既有源码/测试缺口(非本批引入),不需修正。
W0 Task 9a: complete(6098569; 54项(db.table 26+config.setting 11+asset.vendor 13+asset.modelPrompt 4)→26行,7绿/11部分/5缺失/3NA; 待独立审查). Task 9(237项)按体量拆9a(结构/配置54项)+9b(asset.skill技能库183项,尚未派发)两批,非计划原文预拆(计划未细分Task 9子批次,由控制器裁定拆分边界,同6/7批次先例). 方法论亮点: 26张表逐列比对ToonFlow initDB.ts(1055行)与DramaFlow db.dart(401行)确认结构无缺表,对已被Tasks 6/7/8覆盖过行为的24/26张表采用"独立核验结构存在+交叉引用既有行为判定行"而非重新审计(仅o_skillList/o_skillAttribution两张零覆盖表做全新审计,后者判缺失因DramaFlow自己代码注释明载"逐阶段归属映射...被砍不搬"与全仓零读写grep互证). **重要真实发现(静态推断,未跑黑盒)**: asset.modelPrompt 4个视频模板文件中,仅1个(seedance2Multi-parameterMode.md)在engine.dart的_seedBundledModelPromptRows有生产播种代码绑定到volcengine:doubao-seedance-2-0-mini-260615;另3个模板对应"真实"Seedance 2.0非Mini型号的组合,静态追踪未找到任何播种路径,若属实意味着选真实Seedance 2.0型号在text/first_frame/first_last_frame三种模式下生成视频提示词会抛errPromptMissing——已如实标注"未跑黑盒确认""部分实现非缺失非绿"未妄下结论,是独立于今晚已知P0 HTTP 400问题的另一个真实视频管线缺口,值得关注但按会话约束未进一步验证. 对brief原文一处勘误: toonflow.ts核实非本地扩展(author字段字面"Toonflow",initDB.ts基础schema静态播种,有版本门控更新机制),仅azt.ts/ima2.ts的author字段字面"Local"才是真本地扩展,已在行内备注更正. 自我审查抓到4处引用错误(zip文件字节数顺序错位/编造不存在的_AgentBindingsPanel控件类名/prompt常量行号误引/o_prompt默认播种数9误记为漏数2条实为11条)均已修正后再提交. UNIT9A-CLEAR确认,全量孤儿266→212(净−54,范围外零污染独立复核). **独立审查(6098569)判定needs fixes**: 1条Important(atlascloud.ts被整体误归入"私有协议不可达组",但其textRequest实为createOpenAICompatible指向getChatBaseUrl()官方聚合端点,文本模态与minimax同构可达,仅图像/视频走自有submit+poll不可达——审查开atlascloud.ts:429源码逐字核实)+3条Minor(memory-params行"共7项"实为8项/o_skillList播种数"13条"实为21条[7 main+14 references,审查开initDB.ts逐行数清]/deepseek.ts协议引用createDeepSeek实为createOpenAICompatible). 控制器直接Edit修正(未重新派implementer,4处均为精确定位的文本级修正):atlascloud改列W9A-VENDOR-PROTOCOLCOMPAT-001(id单一归属,BESPOKE-001不再重复列出,备注互相说明不留悬空引用)+3处Minor文本更正. **派聚焦复审(非重新全量审计)验证修正**: 3/4项一次到位,复审又抓到1处遗漏(BESPOKE-001"用户可观察"列一处"这5个供应商"未随分组从5→4同步更新,标题/证据/覆盖库存/备注4处均已改对但此处漏改)——控制器再次直接修正(98c4be9),机械复核确认全文"4个供应商"×5/"5个供应商"×0、孤儿检查UNIT9A-CLEAR、12管道符完整. Task 9a最终状态:26行54项,7绿/11部分/5缺失/3NA,经"审查→修正→复审→再修正"两轮收敛,现确认干净.
W0 Task 9b: complete(754429d; 168项(art_skills 132+story_skills 36)→2行[按家族拆分,家族内23个技能包不再逐包拆分],均部分实现; 待独立审查). 方法论亮点: 全168个.md文件SHA-256逐一比对(非抽样)+diff -rq整树递归比对(含32个封面图片)双重验证zip与ToonFlow源码字节级完全一致;完整追踪zip解压(bootstrap_io.dart每次启动无条件解压,fill-gaps-only不覆盖用户自定义)→引擎读取(manuals.dart)→真实可点击选择器(project_dialog.dart两个ManualGallery,widget测试真实tap验证)→6处真实生图/文字生成调用点注入prompt(prompt_resolver.dart,均有engine测试断言切换手册改变解析内容)全链路,证据链完整。**判部分实现而非已验证等价的核心理由(两条均可100%复现)**: ①选择器展示原始技术目录名而非人类可读标题——ToonFlow的getVisualManual.ts按README.md首行动态取名(如"2D_90s_japanese_anime"→"90年代日式动画风格说明"),DramaFlow仅认可选meta.json(168个内置文件0个含meta.json),23个内置包全部显示技术slug原文,仅用户自建包因写入过meta.json而有友好名——是选择"这一确切被审计动作"过程中用户读到的文本发生真实劣化;②ToonFlow前端projectDialog.vue强制建项目必选画风+导演手册,DramaFlow只校验项目名、两字段全nullable无校验,缺失时才在首次实际生成时抛(本地化非崩溃的)errPromptMissing。两条差距均实证非猜测,报告坦承"若无这两条本可判已验证等价",明确留给独立审查裁决是否算作降级理由——与9a的memory-缺失判定同属"实现者给出保守但可能引发讨论的调用"类型,留待审查adjudicate。另确认与9a已判定的db.table:o_artStyle(画风库,轻量名称+封面+提示词条目)是ToonFlow两个不同原生机制(本行审计的project.artStyle实为skills/art_skills包文件夹名,ToonFlow polishAssetsPrompt.ts:61印证),非DramaFlow混淆。自我审查抓到5处行号差一错误+1处错误交叉引用(o_artStyle错挂W9A-DBTABLE-PROJECT-001,应为W9A-DBTABLE-ASSETS-001)均已修正。UNIT9B-CLEAR确认,15个顶层Agent编排提示词文件(production_agent_decision.md等)确认未被本zip涵盖、原样留给未派发的9c批次。**独立审查(98c4be9)approved,零Critical/Important**:审查独立复现168文件SHA-256+diff -rq双重比对(自己的聚合哈希算法与报告不同但结论一致,均字节级相同,判定聚合哈希字符串差异immaterial不影响行内容);逐链验证zip解压→manuals.dart读取→project_dialog.dart两个ManualGallery真实可点picker→prompt_resolver.dart→6处生成调用点全部file:line核实成立;独立裁决部分实现判定成立(非过严):spec §5.1已验证等价要求证明完全等价,两条差距(展示名技术slug非友好标题/选择非强制)均验证为真实可100%复现,不满足等价标准,已验证更优也被排除因slug比中文标题是劣化非优化。审查额外发现第3条支持性证据(非阻塞,已采纳补记入行内备注 e55b3c3):prefix.md在两侧走不同prompt阶段——ToonFlow每次art_*润色固定前置拼接prefix.md,DramaFlow改把prefix.md内容注入"画风风格"字段展示,字节相同但拼装阶段不同,进一步支持部分实现而非上调。3条Minor(报告内聚合哈希字符串算法差异/README首行"# "符号未剥离举例疏漏)均为报告措辞层面不影响checklist本身,不需修正。
W0 Task 9c: complete(69e1ff0; 15项Agent决策/子代理编排提示词内容→4行,全部缺失; 待独立审查). 明确不重审Task 8已定论的机制层结论(DramaFlow无子Agent委派架构),仅审内容层——15个ToonFlow提示词文件(六阶段决策常量/R1-R4监督红线/衍生资产提取规则/大三角矛盾阶梯/股价级反转/付费点公式等)在DramaFlow全仓关键词扫描0命中,assistant_skills.dart自己代码注释承认"被砍不搬:...13个seed"+专门回归测试(engine_facade_test.dart T13)显式断言不自动播种互相印证。独立发现brief未预告的细节:①2个production_skills技法文件走activate_skill二级机制(非13个主系统提示词的直接读入),且技法文件之间可达性不对等(一个被点名激活/一个被其唯一消费方明文禁止激活,因分镜表新旧字段体系不同步已成遗留文件);②DramaFlow存在同名非Agent直连流水线(script_plan.dart/storyboard_table.dart/storyboard.dart)产出物写入与ToonFlow相同的o_agentWorkData键,但追踪resolvePrompt全部4个内容来源后确认方法论内容0%重合(仅槽位名和存储位置巧合一致),这批"槽位存在内容为空"与其余"连槽位都不存在"证据形态不同,独立成行(W9C-PRODSKILL-PIPELINE-001)未强行合并或拔高评级,评级取舍已在行内注明供审查裁定。
**重大发现(9c 例行全量孤儿检查意外暴露)**：Tasks 6-9提交后运行`check_no_orphans.js`全量检查,孤儿数应符合"仅剩Task 10范围"的预期落空——29个孤儿中仅1个是platform.electron:main.js,另外**28个是web.page(15)与web.route(13)**,与Task 9c本身无关,经`git show e55b3c3`独立核验为Task 9c开始前就存在的既有缺口。**这意味着此前记录的"W0 Task 6 全部完成(6a-6e,5批次全部独立审查approved)"结论不准确**——控制器直接复核确认:web.route:*13个id在全文zero覆盖(6a的行标题虽标注路由路径如"（/project）"但覆盖库存列从未正式列入对应web.route:*id,纯记账疏漏非实质审计缺口,但需逐条核实非一律假设);15个web.page多为已审页面下components/子目录的对话框/子组件文件(如views/assets/components/addAssets.vue),另发现views/taskList/index.vue是从未被任何6x批次触及的真正全新页面、views/scriptAgent/index copy.vue是疑似上游dev残留副本文件。/detail此前在Task 3已判定"注释掉的死路由→应入inventory-na.md"但从未真正执行。已派收尾批次Task 6f(28项,不新增独立字母序列而是回填6a-6e系列)专项核实并逐条了结,拒绝纯行政式批量回填、要求逐条验证既有行证据是否真正覆盖该具体行为。
W0 Task 6f: complete(6b87bd5; 28项(web.route 13+web.page 15)→14背书入既有行+5新行部分实现+9条N/A(3路由+6死代码组件); WEB-FULLY-CLEAR确认,全量孤儿最终仅剩1项platform.electron:main.js(Task 10既定范围)；待独立审查). **纪律亮点**:逐条核实拒绝行政式批量回填——11个id(5部分实现+6死代码)本可机械背书进父页面已判绿的行,均因"代码忠实移植但检索不到任何widget级UI测试驱动"或"从未被真正挂载"而拒绝背书、改判更保守的状态,是本任务投入时间最多的部分。**与dispatch假设方向相反的发现**:taskList/index.vue被brief预判"很可能是真实未审计页面需全新审计",实际核实为三重死代码(router未注册+自身import的子组件文件不存在+调用的后端接口不存在),真正"我的任务"能力由已判绿的web.page:views/task/index.vue(W6-TASK-001)承接——未盲从派发方假设,独立验证后给出相反结论。**意外修复的循环引用文档bug**:inventory-na.md既有两条N/A行(generate copy.vue/useChat copy.ts)均以"与已排除的scriptAgent/index copy.vue同型"作为类比论证,但精确核实后scriptAgent/index copy.vue此前从未真正获得自己的N/A行——Task 6b/6e阶段的记录疏漏(A引用B已排除,但B实际未被排除),本次28孤儿列表恰好含该id,顺带补齐后两条既有引用才算名副其实。**根路由/→/workbench两跳重定向vs DramaFlow直接映射**的净效果等价性判定为本批唯一一处"路由确有独立行为需专门论证"的情况(Vue Router绝对路径子路由不拼接语义,已独立验证),零测试佐证根级行为本身(初始路由属一次性状态难以widget测试断言),已诚实标注供审查评估论证强度是否足够。另诚实标注(未修改,按约束只标注不改动陌生人已批准的行)：既有W6-NOVEL-001行"编辑"行为的测试引用经核实其实未被任何真实用例点击触发、引用略显过泛。**独立审查(69e1ff0)approved,零Critical/Important**：11项拒绝背书的判定(taskList三重死代码/circular-reference bug/5个zero-widget-test新行)逐一独立复核全部成立——taskList三条独立证据(router未注册+import的taskDetails.vue确认磁盘不存在+调用的getMyTaskApi确认后端不存在)审查逐条重新验证成立,推翻dispatch假设的判定被审查确认正确;根路由重定向链等价性判定审查独立用Vue Router v4语义重新论证同意等价;9处背书对既有行的diff逐列核对确认只追加覆盖库存/备注两列、状态列字节不变(7个已验证等价+2个部分实现均未被静默篡改)。3条Minor均为如实披露的非阻塞事项(根路由零测试覆盖/W6-NOVEL-001既有引用过泛已排入未来清理/inventory-na.md死代码类目最终仍需W0 §5.4用户范围追认),审查明确"这不是6f的缺陷,是既定6b/6d/6e先例的延续"。**至此Task 6(含6f收尾)真正100%完成,web.*维度零孤儿**。全库存孤儿数从Task 9c发现时的29降至1(仅剩platform.electron:main.js,即Task 10范围)。
W0 Task 10: complete(9318bfe; 1个库存id(platform.electron:main.js)按9个真实facet拆9行,5不适用+2缺失+1部分实现+1不适用(死代码例外); 待独立审查). 控制器预读main.js纠正brief"8个action"为实际9个(getlocallanguage漏计),实现者独立复核确认。**关键推翻控制器预判的发现**：①自动更新并非真的不存在——控制器初读main.js/electron-builder.yml(publish:null)误判"ToonFlow本体无自动更新",实现者深挖发现真实机制在HTTP路由层(checkUpdate.ts/downloadApp.ts)且已被更早的W7A-ABOUT-UPDATE-001判定缺失,本任务范围内的apprestart动作(唯一调用方about.vue:359正是更新流程收尾)据此归入同一缺口不重复计分,未另开新行;②文件导入导出确认非main进程/原生对话框范畴(全仓grep showOpenDialog/showSaveDialog零命中),真实机制是浏览器Blob/File API与HTTP后端交互,5类流程(小说导入/剧本导出/视频导出/分镜下载/候选下载)逐一核实均已被既有行覆盖,无新缺口. **拒绝机械式N/A的两处**：后端就绪权限弹窗+优雅关闭(W10-BACKEND-LIFECYCLE-001)与macOS Dock常驻约定(W10-APPLIFECYCLE-DOCK-001)均验证DramaFlow侧确实空白(main.dart裸函数无try/catch;AppDelegate.swift明确返回true导致关窗即退出,与ToonFlow/Safari/Mail式macOS常驻惯例相反)后判缺失,而非因"这是Electron管件"反射性打N/A。**技术细节留痕**：main.js的windowclose用app.exit(0)而非常规close,按Electron文档该调用跳过before-quit/window-all-closed事件链,可能导致ToonFlow自己的优雅关闭/Dock常驻代码经由其唯一可点关闭按钮很少真正触发——已标注为不影响判定的技术注记，未黑盒验证(受会话约束)。**Task 11外的意外发现**：DramaFlow shell.dart的反馈/GitHub外链目前指向上游HBAI-Ltd/Toonflow-app仓库而非DramaFlow自己的,已备注但不在本单元处理,留给许可证/品牌审计。**里程碑**：`node tool/parity/check_no_orphans.js` 现输出 `OK all 538 inventory items covered`——**W0 Task 6-10 全部538项库存正式清零，内容审计阶段完成，仅剩Task 11(许可证审计,非评分单元)**。
**独立审查(6b87bd5)approved,零Critical/Important**：9个protocol action数量独立重数确认9(非brief的8);自动更新交叉引用链端到端核验(checkUpdate.ts/downloadApp.ts真实存在→W7A-ABOUT-UPDATE-001确认已判缺失且覆盖库存正是这两文件→about.vue:359确认apprestart唯一调用方正是更新流程收尾)——无重复计分无遗漏缺口;文件导入导出抽查2处(小说导入/剧本导出)源码+既有行双重核实confirmed;两处"缺失"判定(权限弹窗+优雅关闭/Dock常驻)双侧独立核实(DramaFlow main.dart裸函数+engine.dart无守卫+AppDelegate.swift明确返回true三处grep确认零命中相关生命周期代码)；窗口 chrome 判N/A经审查独立论证成立(非逃避比较——DramaFlow MainFlutterWindow.swift/AppDelegate.swift确认零窗口定制代码可评,机制本身平台特有且原生macOS窗口管理免费提供,非逃避而是DramaFlow确无代码可评部分实现/已验证等价某一档);外链部分实现的zero测试覆盖声明经shell_test.dart全文核实(4用例均未点击任一链接)成立;app.exit(0)跳过优雅关闭事件链的Electron API事实性描述经审查用自身Electron API知识核实为真非编造。**最关键**：审查独立完整重跑(非grep过滤)`node tool/parity/check_no_orphans.js`确认字面输出`OK all 538 inventory items covered`+exit code 0——里程碑真实成立非仅凭报告断言。3条Minor均不影响任何判定(locale N/A属已披露已有先例的死代码例外/一处旁证计数6实际5的无害笔误/MainFlutterWindow"零定制"措辞对ComposerPlugin注册行略有不精确但行内实际备注措辞本身准确)。**W0 Task 6-10全部内容评分单元完成，经全部独立审查approved，全库存538项零孤儿，唯一剩余为Task 11许可证审计（非评分单元，为L0b决策准备材料）**。

# 收到 Stop hook 反馈后：真机 iOS 集成测试补做（develop 分支，非 w0-audit）
Mac 屏幕锁屏后曾误判"无法继续验证"——实测发现 `flutter test integration_test/*.dart -d <simulator-id>` 走 Xcode 测试桥接（非屏幕自动化/Accessibility 权限），在锁屏状态下对 iOS Simulator 依然完全可用；`xcrun simctl io screenshot` 同理可读取模拟器自身虚拟帧缓冲，不受宿主屏幕锁定影响。据此新写 `app/integration_test/golden_path_navigation_test.dart`（commit `a313950`）：真实启动 DramaFlowApp() 根组件（非合成测试路由）→ 空态确认 → 完整走一遍新建项目向导（真实文本输入+2次下拉选择+持久化断言）→ 进入工作台 → 依次点击全部6个项目标签（小说原文/剧本管理/塑角造景/视频生产/资产中心/剧本Agent，每个标签点击前新增"必须完全在屏幕内"断言，防止滚动精度问题被 warnIfMissed 静默放过）→ 返回项目列表→任务中心→设置→回到项目列表验证数据完整。在真机 iPhone 17 Pro 模拟器上跑通，零 warning。另外通过 `flutter run -d <sim>` 真实生产路径（非测试构建变体）截到一张当前、真实的启动态截图（我的项目空态，UI 干净无异常）。macOS 侧：`flutter run -d macos` 证实 Dart 引擎真实启动成功（Dart VM Service 正常）但因锁屏无法前台化窗口（"Failed to foreground app"，Electron/macOS 窗口呈现需要 WindowServer 处于可交互态，这点与 iOS Simulator 的独立虚拟帧缓冲不同，不可绕过）；对 macOS target 跑同一份 golden_path_navigation_test.dart 因桌面态用 NavigationRail 而非移动端底部 NavigationBar 而按预期失败（非产品 bug，是测试针对移动端布局写的），未临时再写桌面专用版本；转而对全量既有测试套件做了一次全新鲜跑（`flutter test`，非过滤子集）：**598/598 全绿，零跳过**，覆盖 mobile+desktop 两种视口尺寸的既有用例，是当前 develop HEAD（含刚合并的 W0 审计）之上的真实回归基线。`flutter analyze` 干净。清理了本次验证过程中启动的两个 `flutter run` 后台进程。
W0 Task 11: complete(3157505,基于8013903+2d35a73修正; docs/parity/license-audit.md 339行六节+tool/parity/gen_license_trace.js+license-trace.json; 不修改master-checklist.md/inventory-na.md; 独立审查(9318bfe)判needs fixes→已修正→未再派复审,控制器自行核verify LICENSE:257/259逐字确认修正正确). 方法论:§2衍生内容清单未重新审计代码,而是从今晚已完成538项审计中grep"照抄/移植"字样并逐条排除语义相反的"尚未移植"缺口记录,得17行真实衍生内容(而非brief猜测的~180行)——审查独立抽查3+3行排除/收录逻辑均成立. Step1溯源脚本逐字节比对确认default_skills 200/200一致(216个文件并集,15个仅存于ToonFlow侧即Task 9c已确认未随包的15个顶层Agent编排文件)、model_prompts 4/4一致,且200与Task 9b"168个.md"表面数字不同已被独立核实为同一事实不同统计口径(168+32封面图=200,审查独立用find核算文件数确认). **最高风险区域**：§1 LICENSE 逐条引用经审查全篇逐行比对，258行原文中仅§1.6 AGPL不追溯条款一处引用行号偏差2行(255/257误标应为257/259,漏数副标题+空引用行,原文文字本身逐字正确未影响可读性但按brief"零容忍"标准判needs fixes)——控制器直接修正三处行号引用(1.1结构范围/1.6标题/两处内联引用)+直接开LICENSE:257/259复核确认修正后完全一致. 另2条Minor直接一并处理: MIT列表36标签下实际37项(devtools重复计入,已从列表移除,单独MIT*类目在摘要行已覆盖)/§4.2表格"此义务不触发"措辞审查判定"合规但可更稳妥"已按建议改为"条款原文...（满足此条件时）...是否满足由第6节各选项决策时核对"以彻底消除"条款怎么说"与"DramaFlow现状是否符合"两种读法的残余歧义. 防泄漏自检两次假阳性("task-9b-report.md"文件名含"sk-"子串)已用改述而非抑制处理,复跑确认CLEAN. **至此W0 Task 1-11全部完成，全部经过独立审查（Critical/Important问题均已修正或经controller直接核验修正），538项库存零孤儿，许可证审计交付供L0b决策参考（非法律意见）**。

# 用户要求的本次会话全量代码 review（max effort）与修复
Review 范围 dca83dd..HEAD 代码面（产品修复+5测试文件+8工具脚本；~4700行审计文档已各自经独立评审不重复审）。10个查找角度→4个批量验证器（含真实起进程攻击复现）→1轮扫尾：38候选→15条经验证存活（全部CONFIRMED），另2条被验证器证伪（fake_upstream缺error handler会崩进程——实测现代Node自行消化RST不崩；golden_path文件头"过度声称"——文件头本就写明引擎测试隔离）。15条已全部修复或锁定：
1. **engine.dart prompt()改为upsert**（TDD:先红后绿）：data列始终跟随当前代码种子文本、useData用户定制不动——修复"收紧后的分镜表提示词永远到不了老库"（原来updatePrompt只写useData/resetPrompt只清useData/clearAllData保留o_prompt/迁移只改schema，四条路全堵死）。新回归用例：二次boot旧data被刷新+useData保留（engine_facade_test 27/27绿）。
2. **storyboard.dart删死代码trueValues**：简化为raw.isNotEmpty && !falseValues.contains(raw)（验证器穷举案例分析证明语义完全等价），注释明示已知限制（精确token匹配，含否定词整句/全角/零宽污染落默认true）。新增测试锁定空单元格→false边界+全角Ｎ/零宽空格→true现状（有意识记录已知限制）。
3. **golden_path集成测试两处缺陷修复**：①tab查找scope到AppBar（此前find.text('小说原文').first命中novel_screen内层子tab——Scaffold元素树body先于appBar，第一轮点击是无效no-op，验证器写探针测试实证）；②固定手机竖屏视口（此前≥840dp桌面壳无BackButton、标签只存在于Tooltip会硬失败）。真机iPhone 17 Pro模拟器复跑通过。
4. **P0 live harness四处修复**：o_video轮询限定本次storyboardId+ORDER BY id DESC（防跨次运行历史行污染"恰好一次"判定）；P0_PHASE非法值直接fail拒绝假绿；addTearDown(engine.dispose)堵住所有失败路径的引擎泄漏（exit(9)硬杀是唯一有意例外）；参考图fixture existsSync守卫+说明。
5. **P0 drill B"核心断言"落实**：candidatesBefore/After+1、新行id≠原行id、原行submissionState/upstreamTaskId原样保留（不可变历史）——此前只print不assert，复用原行的回归会静默通过。两个drill对假上游实测全绿。
6. **full_pipeline**：_waitTask allowFail补齐超时分支（今晚23张批量生图超时击穿整条链路的正是这个缺口）；两处轮询谓词改单次查询缓存。
7. **sanitizing_proxy**：控制端点改pathOnly匹配（/__health?x=1不再漏到真实上游）；proxied error handler加headersSent守卫（上游中途重置不再ERR_HTTP_HEADERS_SENT击穿整个代理，验证器实测曾复现崩溃exit 1）；up/req/res补error/close清理（客户端中断不再泄漏对真实付费API的连接）；/__mark改Buffer.concat。
8. **fake_upstream/proxy_selftest改Buffer.concat**（跨块多字节UTF-8不再解成U+FFFD腐蚀capture审计文件）。
9. **proxy_selftest加固**：新增3条真实断言（控制端点带query本地应答/query原样到达上游/日志行键集合结构性白名单校验——替代原三条恒真的字符串grep）；req()补5s超时；killAndWait等exit事件替代sleep(200)防EADDRINUSE；全程children登记+finally收尸防泄漏子进程掩盖根因。16/16自测通过，SENTINEL已按新代理哈希重签。
10. **check_no_orphans改未转义竖线切分**（(?<!\\)\|）——标准markdown \|转义不再静默错位覆盖库存列（纯增强，现有数据零\|占用，复跑仍OK all 538）。gen_license_trace unzip包try/finally防临时目录泄漏（复跑输出仅时间戳差异，产物未变已还原）。
未修（记录在案待裁决/后续）：AGENTS.md line21"测试禁真实API"与P0/QA两个env-gated真实测试的规则冲突（属用户裁决：补豁免注记或挪目录）；gen_inventory正则不剥注释（一次性脚本、产物已冻结且/detail案例已被6f妥善处理）；gen_runtime_capabilities恒真assert（一次性已跑脚本、产物已独立防泄漏核验）；测试助手三处重复（W0/P0证据文件已冻结，重构反而扰动已审代码）；测试硬编码中文串（全仓~20文件既有约定）。

# 屏幕解锁后补做的 macOS 真实端到端走查（stop hook 反馈项）
早前 4am 锁屏挡住的只有 macOS 窗口前台化这一件事，解锁后已补齐：
- 新写 `app/integration_test/golden_path_desktop_test.dart`（commit fa93e55）：宽视口固定→AppShell 走桌面壳 _DesktopShell（左图标栏+顶部项目菜单栏），真实 macOS 进程上端到端驱动——空态→完整建项目向导→顶部菜单栏点全部6个项目标签→侧栏图标点任务中心+设置→folder 回项目列表。处理了桌面态'小说原文'标签撞名（顶部菜单 vs 小说页内层 TabBar）：先导航离开小说路由再进循环，保证每个标签点击时唯一。macOS(darwin-arm64) 隔离复跑 All tests passed。
- **真实 macOS 应用窗口截图已取到**（screen 解锁后 flutter run -d macos 真正前台化，此前锁屏时的'Failed to foreground app'纯属 WindowServer 需可交互态）：桌面壳渲染干净——左图标栏、顶部项目菜单栏(小说原文/剧本Agent/剧本管理/塑角造景/视频生产/资产中心)、我的项目 header+暂无项目空态、原生 macOS 窗口 chrome，无视觉异常。
- iOS 侧此前已有真机模拟器 golden_path 集成测试通过 + 真实启动截图。两端 UI 走查现均有真实证据。
- flutter analyze 干净；单元/widget 全量套件仍 599 绿（集成测试不进默认 flutter test，需设备单独跑，mobile+desktop 两个 golden_path 均已在各自设备上单独验证通过）。
- 用户在此机器活跃使用中，未持续抢占窗口焦点；跑完即清理 flutter run 进程。
- 仍未做/受限：物理 iPhone 真机（只有模拟器，未连真机）；视频最终生成（用户明确指示搁置）。

# 移动端适配审计 + W1/W2/W3 后续里程碑参考文档（新调查，与 W0 538 项清单完全独立）
用户指令：继续对比 ToonFlow 与 DramaFlow 的功能对齐度，同时审计 DramaFlow 自身移动端适配质量；遇到卡住的地方先记录不修，之后由用户处理；合理用 subagent 并行加速。本阶段为纯只读审计+文档产出，未修改任何生产代码或 `docs/parity/master-checklist.md`。

**移动端适配审计**（7 组 subagent 并行，覆盖 `app/lib/src/screens/**` 全部 31 个屏幕文件 + shell/canvas/table/dialog 共享组件）：
产物 `docs/parity/mobile-adaptation-findings.md`。核心发现：
- 积极架构发现：`DFCanvas` 在 <840dp 时被有意禁用改用 Tab 布局，直接解决了本次调查最初动机"无限画布体验不好"；`storyboard_canvas_node.dart` 的长按菜单是对 ToonFlow 悬停交互的真实改进（文件头注释自证）。
- 类别 A（严重）：悬停专属编辑/删除控件在触屏上完全不可达，同一错误独立重复 3 次——`manual_gallery.dart:143-161`、`art_style_library.dart:156-219`、`script_screen.dart:453-462/500-510`。
- 类别 B：`script_screen.dart:467` 固定 width:400 卡片在所有手机宽度下数学上必然溢出；`event_tab.dart:89-118` 工具栏无响应式处理近乎必然溢出；`corner_scape_screen.dart:236-286` 行挤压已通过真实探针测试实测确认（非推测）。
- 类别 F（本轮新发现）：`agent_chat_screen.dart` 在手机 `_MobileShell` 内叠加自身 AppBar 与 shell 的 AppBar+Tab 条，约 220dp chrome 吃掉小屏三分之一高度，`agent_chat_screen_test.dart` 从未挂载在 `AppShell` 内因而测试网完全捕获不到。
- 另有 D 类（移动卡片相对桌面表格丢数据/丢操作，`event_tab.dart` 移动卡片甚至丢失了单条删除能力）、E 类（非滚动容器+真实数据量导致内容不可达，`assets_screen.dart`/`batch_generation_dialog.dart`）、C 类（触控目标<44px，`workbench_screen.dart` 裁剪手柄仅 14px 最极端）详见产物文档。
- 系统性测试方法论缺口：多个"已测手机视口"用例统一用 390×900（高度超过 iPhone SE/13 mini 实际可用高度）+ 种子数据仅 0-3 条，掩盖了 E 类问题。
分组原始笔记（含 Minor 级发现全文）：`/private/tmp/claude-501/-Users-luke-Documents-aivideo/32c05c60-7be8-4f4d-8fec-9ff46fd45e55/scratchpad/mobile-audit/01~07-*.md`（会话临时文件，未迁移进仓库）。

**W1/W2/W3 后续里程碑参考文档**（read-only 综合已完成的 W0 审计发现 + 针对性补充调查，非实施，供未来 spec 起草用）：
- `docs/parity/w1-canvas-reference.md`：canvas 性能与手势体验指标参考。源码举证 ToonFlow 画布卡顿的 7 个具体成因（关闭虚拟化/storyboard 节点无窗口化/interaction-mode 剥离交互元素/常驻 FPS 计数器/空间拖拽无 rAF 节流/深度 watcher/逐格 hover 反应性），并区分 DramaFlow 两个画布场景（主生产画布固定 6 节点 vs editImage 节点编辑器可变节点数+storyboard 内部图格）供未来性能指标制定参考。
- `docs/parity/w2-agent-reference.md`：Agent 体系移植范围参考。综合 W0 已确认的 scriptAgent/productionAgent 双家族架构、决策层编排、子 Agent 生成、3 层记忆子系统、Markdown 技能懒加载等发现，含 `vm.ts:1,47-55` 的 vm2 解释器实证引用，供未来 W2 spec 精确圈定范围（明确排除恢复 JS 解释器/ES-DSL 等已废弃方案）。
- `docs/parity/w3-nle-reference.md`：NLE 工作台缺口参考。W3 gap-reference subagent 派发因基础设施瞬时故障（"Connection closed mid-response"）连续失败 3 次（均在长任务接近尾声时中断），第 3 次失败后改为控制器直接调查（grep checklist + 读 ToonFlow `mediaData.ts` 源码 + 读 DramaFlow `compose.dart`/`ComposerPlugin.swift`(macOS+iOS，除插件注册样板代码外逐字节相同)/`MainActivity.kt`(Android) + workbench UI 下拉框）完成。核心结论：**spec"解冻现有被隐藏能力"的措辞未获证实**——转场(3 vs 6)/滤镜(4 vs 10，且同名项算法也不同)/特效(0 vs 8+2贴纸) 在 Dart 校验层、macOS/iOS/Android 三端原生合成器、UI 下拉框四处完全对齐，没有"引擎已实现、UI 未暴露"的低成本解冻机会，真正缺口需要三端合成器新增实现。另有自由剪辑对比（DramaFlow 叠加层模型 vs ToonFlow 自由多轨；DramaFlow 独有波纹/分组/吸附能力 ToonFlow 没有；DramaFlow 缺编辑器内撤销重做+实时预览；Web/Windows/Linux 无合成器）与按成本分组（A 组可能是纯 UI 工作待核实 schema、B 组需三端合成器新实现）的具体缺口清单。

未修/未做（本阶段不实施，记录待裁决）：W1/W2/W3 对应的任何实际实现——spec 明确"未过 L0b：W1–W4 不开工"，本阶段仅产出参考文档不构成开工。移动端适配缺陷本身已在下一阶段全部修复，见下条。

# 移动端适配缺陷修复（用户 /goal 明确要求"你说的所有功能都处理好"，追加于审计之后）
用户明确指示把审计发现的移动端缺陷实际修掉（区别于此前"先记录不修"的指令，本条为新的、更明确的指示）。11 个独立文件并行派发 11 个 implementer subagent（文件互不重叠，安全并行），每个都：实现修复→跑对应/新增回归测试→`flutter analyze`。完成后控制器跑了全仓 `flutter analyze`（0 issues）+ 全量 `flutter test`（613/613 全绿，无回归）。范围明确不含 W1-W4（画布重写/Agent 移植/NLE 转场滤镜特效新增实现），L0b 门槛不变。

修复清单（文件:关键改动）：
1. **manual_gallery.dart**：悬停陷阱→`showActions = _hover || compact(<700dp)`（复用 `project_list_screen.dart` 既有正确范式）；`_MiniIcon`/新建按钮触控目标补到 44dp。新增 `manual_gallery_test.dart`（此前零覆盖）。
2. **art_style_library.dart**：同一 bug class 同一修法；`_MiniIcon` 包 44x44 命中区保留视觉小巧。新增 `art_style_library_test.dart`（此前零覆盖），同时验证桌面 hover 行为未回归。
3. **script_screen.dart**：①400px 固定宽度卡片改 `LayoutBuilder` 响应式 `clamp(0,400)`——数学上保证溢出的 bug 修复；②删除图标同 `showActions` 范式脱离纯 hover。
4. **event_tab.dart**（本次审计最严重的功能性倒退）：①工具栏复用 `novel_screen.dart` 的 `compact<720` Row→Wrap 模式修溢出；②移动卡片补回单条删除（含二次确认），不再强制走批量选择。新增 `event_tab_test.dart`（此前零覆盖）。
5. **corner_scape_screen.dart**：复用文件自身已有的 `<420` Row→Column 断点范式，长角色名不再逐字换行挤压成畸形高行。回归测试用 10 字符真实案例（云梦泽畔听雪楼二当家），且用"临时回退验证测试真能抓住 bug"的方法核实过。
6. **agent_chat_screen.dart**（本轮新发现的 bug）：`MediaQuery.sizeOf(context).width<840` 判定是否在 `_MobileShell` 内，是则不再渲染自身 `Scaffold`/`AppBar`（actions 移入新增的 `_InlineActionsBar`），桌面 `_TopBar` 路径逐字节不变。修复过程中发现并顺带修了移除 Scaffold 后 Switch 缺 Material 祖先的连带问题。新增测试通过真实 `GoRouter`+`ShellRoute`+`AppShell` 挂载验证只有一个 AppBar，并用 git stash 回退验证测试真实有效。
7. **novel_screen.dart**：移动卡片补章节内容预览（`contentCell(maxLines:2)`）；`_showDetail` 从裸 `AlertDialog` 改用全仓统一的 `showDFAdaptiveDialog`。
8. **import_novel_dialog.dart**：同款章节预览补齐（step2 选章界面，此对话框核心用途就是选章，此前是盲选）。新增 `import_novel_dialog_test.dart`（此前零覆盖）。
9. **batch_add_dialog.dart**：移动卡片标题补 `maxLines:1/ellipsis`（对齐桌面同字段已有处理）；顺带把固定 430 高度改 `ConstrainedBox(maxHeight)` 允许收缩。
10. **batch_generation_dialog.dart**：①移动卡片标题截断对齐 `assets_screen.dart`；②固定 430px 不可滚动表格区改 `Expanded`+`SingleChildScrollView`，图片模式下真实数据量不再溢出/不可达。
11. **assets_screen.dart**（审计中影响面最大的单一 bug："任何资产数超过几个的项目的默认状态"都会触发）：call-site 级修复（未动共享的 `DFDataTable`，因为还有 5 个其他调用方），840dp 以下包 `SingleChildScrollView` 给移动列表补上缺失的可滚动祖先。回归测试用真实 iPhone SE 尺寸(390×667，而非全仓此前普遍误用的 390×900)+满页10条数据验证可滚动触达。

系统性方法论副产品：多个 implementer 在修复中沿用"临时回退修复→确认新测试真的会红→再恢复→确认变绿"的验证法，不是单纯写一个总为真的断言。`agent_chat_screen.dart` 的修复还意外发现 flutter_test 默认视口（800×600）本身就低于 840dp 断点，意味着此前"未设置手机视口"的测试其实也一直在（意外地）跑桌面分支之外的路径——该发现未引出新增修复，但供后续测试方法论参考。

# 供应商预设体系 subagent-driven-development 执行（plan: docs/superpowers/plans/2026-07-18-provider-presets.md v2）
Task 1: complete (63c84dc + 修复 06dd98c; provider_presets.dart 12 家目录+种子单一化, engine.dart 种子改从目录构建; 一审 Important×2——① deepseek-v4-flash/pro 与 kimi-k2.6 疑似撞了 Toonflow-app/data/vendor/{deepseek,atlascloud}.ts 的既有字符串(早于本 commit 5 周)，需独立复核排除巧合以外的可能；②azt acceptanceVerified 注释误指向尚未跑的 Task 7。修复：①次日(2026-07-19)不接触 Toonflow-app 任何文件、重新 WebFetch+WebSearch 独立复核，deepseek 官方文档确认 deepseek-chat/reasoner 将于 2026-07-24 15:59 UTC 弃用转 deepseek-v4-flash(非思考)/思考模式，v4-pro 经独立定价页+多源交叉确认为真实独立旗舰档；moonshot 官方文档确认域名迁移 platform.moonshot.cn→platform.kimi.com 属实，kimi-k3(2026-07-16 发布)/kimi-k2.6 均为当前真实型号——两家结论均与原实现一致但改为真正独立佐证(非同一巧合);②注释改指向本 session 早前已存在的真实证据(progress.md「P0 Task 4」「QA全链路最终结果」+ docs/parity/p0-provider-preflight.md「azt服务冒烟」+ /tmp/p0-azt-smoke.txt)，Task 7 定位改为"转录"而非"新验证"。二审逐条对照 progress.md/p0-provider-preflight.md 原文核实引用真实存在，approved。4/4 目录测试 + 354/354 engine 套件 + 617/617 全量套件绿，flutter analyze 0 issues。Minor(未修，不影响): commit body 一处英文残留措辞；deepseek 定价倍数 report 内 3.1× vs 聚合搜索"12.4×"未互相协调(不影响代码，代码采用官方定价页更权威的 3倍数值)。)
Task 2: complete (2d18c37; createProviderFromPreset INSERT 先行抢占——SELECT+INSERT 连续同步无 await 段抢占供应商行、凭证只在抢占成功后才写、凭证失败删行；errProviderExists 新增。实现者用探针脚本实测 SqliteException.extendedResultCode(1555 PK冲突/2067 UNIQUE/1811 RAISE(ABORT)触发器)排除了误判可能，并做了变异测试(临时改回原始有 bug 的顺序→确认新测试真的会红→改回→确认与最终提交字节一致)。修复了 brief 原文里两个编译错误(EngineException.code 不存在应为 .errKey；ProviderInfo 缺 import)与一个过期测试夹具(brief 假设的 deepseek-chat 已被 Task1 真实核实替换为 deepseek-v4-flash)。审查逐行独立核对抢占段真无 await、约束码分类正确排除触发器误判、并发测试单独重跑通过；approved，零 Critical/Important。Minor(未修，超出本任务范围，供后续参考): 现有 createProvider(非本任务新增路径)仍是"先写凭证后 INSERT"的旧顺序，与新路径原子性不一致；回滚 DELETE 本身无进一步保护(极端情况下若 DELETE 失败会掩盖原始错误)；凭证失败测试无法从外部区分"插入后回滚"与"插入前失败"，已有独立验证补足，非阻塞。6/6 新测试逐个通过+360/360 engine 套件绿，flutter analyze/format 干净。)
Task 3: complete (7d4c31e + 修复 7ffc380; ProviderGateway.listRemoteModelIds(抽象默认 Future.error(UnsupportedError) 同 submitVideo 范式)+HttpProviderGateway 真实现(GET {baseUrl}/models 解析 data[].id)+Engine.fetchProviderModelCandidates 薄转发；只出候选不写库(grep 确认零 db.execute/credentials.write)。实现者主动做变异测试(忽略响应体/丢弃DioException映射/去掉格式守卫，确认3处均被测试捕获)。一审 Important×2(均溯源到 brief 原文代码片段而非实现者偏离)——①凭证读取失败被无条件吞掉、非loopback供应商无Key时仍发出未认证请求，导致401/403被归入笼统errNetwork而非其他真实调用路径统一使用的errProviderMissing清晰错误；②/models 请求无 receiveTimeout，是 providers/ 目录下唯一没设超时的 dio 调用点，而这个方法恰是用来探测可能配错的新 baseUrl。修复：①resolve.dart 的 _isLoopbackBaseUrl 改公开(纯改名，numstat确认仅2行)供 gateway.dart 复用，完整复刻 _resolvedModel 的 loopback 判定逻辑(非loopback缺Key→errProviderMissing，请求前拦截)+新增两个测试(假adapter在被调用时会throw以证明guard确实在网络请求前拦截；真实捕获Authorization header值断言)；②加 receiveTimeout: 20s 对齐同目录其他5个 dio 调用点惯例。二审逐项独立核对：改名后 grep 确认零遗留旧引用，两处guard逻辑与参照实现并排比对确认等价，重跑7/7测试真实通过，analyze 干净；approved。(注：本任务的二审因会话中途一次进程意外退出而重新派发一次，读取被中断agent的transcript确认它只进行到调查阶段、无可复用产出、且审查本身只读不改工作区，重新派发无副作用。)
Task 4: complete (e543c9b; 17 个 l10n 键(zh/en/ja 三语，逐一 JSON 解析确认各语言值互不相同非复制占位)+provider_preset_gallery.dart(showProviderPresetGallery 画廊，12 预设卡+自定义卡共13格，角标逻辑：已添加优先(只显示"已添加")，否则未过验收显示"未验证"+兼容模式(anthropic/gemini/xai)可与未验证共存)。实现者未发现任何 brief 假设的 API 形状(DFColors字段/context.l10n/import路径/showDFAdaptiveDialog签名)有误，零修正直接落地。审查逐案例(openai只未验证/anthropic未验证+兼容双徽章/azt零徽章/已添加只显示已添加)手工推演代码分支并对照 Task1 真实数据核实，全部吻合；approved，零Critical/Important。Minor(未修，均溯源到 brief 原文而非实现者偏离，供后续参考): 徽章视觉样式(扁平边框)与全仓其他徽章惯例(药丸形+语义色底)不一致，未过验收和兼容模式视觉上无区分度；测试覆盖窄于设计稿测试清单(未断言总格数=13、兼容模式角标只测了 anthropic 阳性+openai 阴性两例非全部12家)，经代码走查确认底层逻辑是无特化的统一字段读取，风险低。3/3 新测试绿，analyze 干净。(注：实现者发现全量 flutter test 默认并发下会静默丢失约12/78测试文件仍报"全绿"的既有工具缺陷，与本任务代码无关，已用 --concurrency=1 隔离确认非本任务引入；Task 7 收尾全量回归需注意此坑，不能只看 exit code。)
Task 5: complete (05b0ab5 + 修复 8f805c5; provider_preset_form.dart(预填表单，name/baseUrl 覆盖走 TextEditingController 实时值→createProviderFromPreset→engine effectiveName/effectiveBaseUrl→INSERT，非早期设计稿缺陷重演)+settings_screen.dart 三路接线(已添加id→编辑弹窗/custom→原裸表单原样保留/新预设id→本任务新表单)。一审 Important×1：保存失败时把原始异常字符串(如"errProviderExists {providerId: deepseek}")直接展示给用户，未走全仓统一的本地化错误文案惯例。修复：common.dart 的私有 _engineErrorText 改公开(grep确认改名前仅2处引用皆在同文件内，纯改名零行为变化)，errProviderExists 专门映射到 Task4 已有的 presetProviderExists 键，其余异常走已存在但此前未用的 projectMsgOperationFailed 通用键；新增测试真实触发重复创建异常(非mock)断言本地化文案出现+原始errKey/params不出现。二审逐项独立复核：改名前后 grep 确认零遗留下划线引用、零其他调用方受影响，l10n 键均为 Task4 早已存在的真实键(git log -S 溯源确认非本次臆造)，回归测试重跑3/3绿；approved，零新增问题。(注：Task5 修复期间用户用自己的 git 身份直接提交了本会话更早期的移动端修复改动(b7cff5d+ab634c4)到 develop，与本任务无冲突，仅需把 review-package 的 diff 范围从失效的原始 base 改正为修复提交自身的父提交，才能拿到干净的审查 diff——过程记录见对话，无数据丢失。)
Task 6: complete (29b90f4; settings_screen.dart 模型管理页新增"从 API 拉取"按钮+_CandidateSheet 底部弹层——已知 ID(presetModelKinds 命中)预填 kind、未知 ID 留空("未分类")绝不默认猜 text，勾选但未定 kind 时"加入清单"就地报错不关闭弹层，通过校验的候选以 _ModelDraft(enabled:false) 追加(默认禁用需用户手动开)。实现者顺带发现并修了 AppBar 操作行在移动宽度下的真实 RenderFlex 溢出(复用文件已有的 width>=820 断点做宽/窄两态，非临时band-aid)，以及2处因此产生的过期测试断言(单字段查找→双TextField场景须用findsNWidgets(2))。brief 假设的 deepseek-chat 已被 Task1 真实核实替换为 deepseek-v4-flash/pro，测试夹具同步。审查独立验证：presetModelKinds 命中链路(provider.id确实等于preset.id，非测试巧合)、校验代码路径真拦截"已勾选未分类"场景(非部分放行)、enabled:false构造点定位确认、宽度断点复用而非新造、14/14测试+analyze独立重跑通过；approved，零Critical/Important。(注：实现者工作期间发现 settings_screen.dart 未预期改动，经核实是用户本人正在手动实现新增的 desktopOnly 平台过滤特性(见下)，未提交、与本任务commit(仅2文件)无交集，非重复派发。)
供应商预设体系 6/7 功能任务全部完成，Task 7(全量回归+人工验收清单)进行中——同时注意：用户已在设计稿(2026-07-18-provider-presets-design.md)追加 desktopOnly 字段需求(azt 因是本机 loopback 不应出现在 iOS/Android 画廊)，并已在工作区手动实现(provider_presets.dart/provider_preset_gallery.dart/其测试，未提交，54行改动)——这是原 plan v2 未覆盖的用户追加需求，不属于本次 subagent-driven-development 7 任务范围，Task 7 的全量回归会自然包含这部分工作区代码(flutter test/analyze 读磁盘不看 git 暂存区)，但不会去 commit 它，也不代表它已过本流程的审查——如需要，应作为独立后续任务走完整流程。)
Task 7: complete (commit HEAD; 全量回归——`cd app && flutter analyze` 全项目 0 issues；`flutter test --concurrency=1` 前台同步阻塞跑完(不依赖后台/Monitor异步通知，直接等进程退出)，762 行完整日志，82/82 测试文件真实加载(与磁盘 `find test -name '*_test.dart'` 结果逐一 diff 完全一致，排除 Task4 记录过的静默丢文件坑)，652/652 测试绿、0 失败、0 跳过，exit 0，`All tests passed!`。新增 `docs/parity/provider-presets-acceptance.md`：与 brief 原文逐字节 diff 确认一致，12 预设人工验收表，azt 行转录本会话早前 P0 Task 4 与 QA 全链路真实证据(①②③④全绿)，其余 11 家统一"待验"，与 `provider_presets.dart` 现状(仅 azt `acceptanceVerified: true`)及画廊`!preset.acceptanceVerified`未验证角标逻辑一致。供应商预设体系 plan v2 全部 7 任务完成，均逐一独立审查通过。环境事实(按归因规则核实后确认不阻塞、均未修改任何一处)：①上条记录的 desktopOnly 需求已由用户自己独立 git 身份提交为 743dd6a(provider_presets.dart/provider_preset_gallery.dart+其测试)，另有纯文档提交 fefebef，两者均早于本任务开始已在 develop 上，回归范围自然覆盖，非本任务产物；②用户当前仍有未提交的首次引导功能在制(`app.dart`/`bootstrap_io.dart`/`engine/config.dart`新增`onboarding.completed`配置键/`engine.dart`新增`onboardingCompleted`/`completeOnboarding()`/`settings_screen.dart`仅新增`initialSection`可选构造参数，与 Task5/6 已合入的画廊接线、模型管理"从 API 拉取"代码零交集/三语 arb+生成文件新增 19 个`onboarding*` l10n 键，均在 Task4/5 已有 17 个`preset*`键之后纯追加/新文件`first_run_guide.dart`+3个新测试文件)，首次 `flutter analyze` 曾报 2 条(unused_local_variable于`first_run_guide.dart:31`、deprecated_member_use于`onboarding_test.dart:8`)，复跑时用户已自行修好，最终 0 issues，均非本任务改动。方法论记录供后续参考：首次 `--concurrency=1` 尝试在 `onboarding_router_test.dart` 处卡住 8+ 分钟零进展，一度错误地等待"后台任务完成会自动通知"（本环境对子代理不成立），`ps aux` 排查后发现同一 app 目录当时另有用户自己独立起的 `flutter test --concurrency=1` 进程在跑(非本任务发起，推测系用户编辑器保存触发)，判定为资源争用而非代码缺陷——未触碰用户进程/文件，短暂等其自然结束后重跑一次干净的、前台阻塞的 `--concurrency=1`，一分半内完整跑完得到上述真实数字。教训：子代理起后台/Monitor 任务后不能假设会被动唤醒，必须自己前台阻塞等待或主动核验进程存活，且需用 `ps aux` 而非猜测代码有 bug 来排查"零进展"。)

# 移动端适配平板回归（2026-07-19）
对前一批 390px 手机修复追加 760–800dp 移动壳验证：发现并修复项目/画风/手册/剧本卡片的 `<700dp` hover 误判（700–839dp 触控平板无法编辑删除），统一按 AppShell `<840dp` 移动边界常显操作；剧本工具栏在 760dp 实测溢出 327px，按实际内容宽度 `<1040dp` 换为搜索+Wrap；小说工具栏在同宽度实测 0.4px RenderFlex 溢出，`<840dp` 换行。资产与事件工具栏同宽度实测无异常。项目向导测试改为点击实际 GestureDetector，并把点击命中警告设为失败，避免测试假绿。完整证据见 `docs/parity/mobile-adaptation-findings.md` 第五节。`flutter analyze` 0 issues，`flutter test` 642/642 绿，`node tool/parity/check_no_orphans.js` 538/538。真实视频生成未调用；QA 真实链路需 `QA_FULL=1` 才会执行且源码明确排除视频。
